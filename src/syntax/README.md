# PDF syntax

This module will contain the lexer, trailer and cross-reference resolution,
indirect objects, object streams, incremental updates, and lazy page-tree
navigation. It depends only on the random-access byte store.
