# Page content

This module will interpret page content operations, inherited resources,
graphics state, fonts, encodings, CMaps, and positioned text. Its stable output
is geometric page data rather than plain text or HTML.
