# Page content

This module interprets page content operations, inherited resources, graphics
state, fonts, encodings, CMaps, and positioned text. Its stable output is
geometric page data. Text and HTML serializers consume that data separately.
